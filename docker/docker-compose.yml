version: '3'
services:
  stats:
    image: pytorch_ubuntu_env:v2.3
    user: jovyan
    ports:
      - "8000:8000"
    volumes:
      - ../:/home/jovyan/workdir
    deploy:
      resources:
        reservations:
          devices:
          - driver: nvidia
            capabilities: [gpu]